# Copy to bianca/.env on the Mac mini. Never commit .env.

# Absolute path to this repo on the mini (used to store the WhatsApp Web login).
REPO_DIR=/Users/YOU/path/to/goblin2929.github.io

# Localhost port the WhatsApp sender listens on (Bianca curls this on approval).
WA_SENDER_PORT=8787
